---
type Props = {
  url: string
  name: string
}

const { url, name } = Astro.props;
---

<a href={url} target="_blank" class="text-slate-600 dark:text-slate-300 text-base/6 hover:text-white border border-blue-300 hover:border-blue-600 hover:bg-blue-600 focus:ring-2 focus:outline-none focus:ring-blue-300 font-medium rounded-lg px-4 py-1 text-center dark:border-blue-600 dark:hover:text-white dark:hover:bg-blue-600 dark:focus:ring-slate-600">Install <span class="sr-only">{name}</span></a>