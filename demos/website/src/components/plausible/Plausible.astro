---
import { getCollection } from 'astro:content';

const result = await getCollection('plausible')
---

<p class="text-slate-600 dark:text-slate-400 text-base/6">Return value of a custom query:</p>
<div class="text-slate-600 dark:text-slate-400 text-base/6 flex flex-wrap flex-row gap-4"><pre><code data-testid='plausible-item'>{JSON.stringify(result[0].data, null, 2)}</code></pre></div>